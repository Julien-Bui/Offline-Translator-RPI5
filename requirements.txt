# --- Interface Graphique (UI/UX) ---
customtkinter==5.2.2
Pillow==10.2.0

# --- Reconnaissance Vocale (ASR) ---
# Note : On utilise sounddevice + numpy pour capturer le micro proprement
sounddevice==0.4.6
numpy==1.26.4
faster-whisper==1.0.1

# --- Traduction Machine (NMT) ---
# Argos Translate utilise ctranslate2 sous le capot pour une vitesse maximale sur CPU
argostranslate==1.9.6

# --- Synthèse Vocale (TTS) ---
# Piper-phonemize et piper-tts (à installer via pip ou binaire selon l'OS)
piper-tts==1.2.0

